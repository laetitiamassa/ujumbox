<!-- View if the profile is civil -->
<p class="lead text-center">
	<i class="fa fa-heart fa-lg blujumbox fa-border"></i>
	<%= render 'themesusers' %>
		</p>
<div class="row">

<!-- LEFT -->
	<div class="col-md-4">
					
				
				<p class=" lead text-center"><b>Candidats ou mandataires suivis</b></p>
			
				<%= render 'followedusers' %>

	</div>

<!-- RIGHT -->
		<div class="col-md-8">
			<div class="row">
				<p class="lead text-center"><b>Mes suggestions</b></p>
			
				<% if @user == current_user %>
					<div class="col-sm-6 col-md-6">
						<%= render 'ujumbes/shortform' %>
					</div>
				<% end %>

				<% if @ujumbes.any? %>
				<%= render 'ujumbe' %>
				<% else %>
					<% unless @user == current_user %>
						<p class="lead text-center"><%= @user.name %> n'a pas encore publié de suggestion</p>
					<% end %>
				<% end %>

			</div>
	

	<!-- FOLLOWED UJUMBES -->
			<div class="row">
	
				<p class="lead text-center"><b>Propositions et suggestions suivies</b></p>
		
				<%= render 'followedujumbe' %>
			</div>

		</div>

</div>